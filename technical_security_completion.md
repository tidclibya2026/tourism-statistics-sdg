# تشطيب تقني وأمني للمنصة

## نطاق التدقيق

يركز هذا التشطيب على **المنصة المنشورة نفسها**: المصادقة، الصلاحيات، حوكمة البيانات، التحقق من الإدخال، حدود الطلبات، معالجة الأخطاء، التبعيات الإنتاجية، وإجراءات التصدير. لا يمثل التقرير اختبار اختراق خارجي، ولا يغيّر إعدادات الاستضافة أو أسرار البيئة التي تديرها المنصة.

## آلية التشغيل المعتمدة

تعمل المنصة عبر React في الواجهة وExpress/tRPC في الخادم، مع قاعدة MySQL/Drizzle ومصادقة OAuth. تمر كل طلبات البيانات عبر إجراءات tRPC محددة بمدخلات Zod، ولا يُسمح بإدخال قياسات مدن أو اعتمادها من الواجهة العامة. تُعرض للزوار القياسات المعتمدة فقط؛ أما الإدخال والاستيراد والمراجعة والاعتماد وإدارة المستخدمين فتتطلب جلسة وصلاحية مناسبة.

| طبقة العمل | الضابط المطبق | الأثر التشغيلي |
|---|---|---|
| المصادقة | رمز جلسة موقّع ومربوط بمعرّف التطبيق | يرفض رمزاً صادراً لتطبيق آخر حتى لو كان موقّعاً بمفتاح معروف. |
| ملفات الارتباط | `HttpOnly` و`Secure` عند HTTPS و`SameSite=Lax` | تقليل إرسال الجلسة في الطلبات المتقاطعة غير الآمنة مع بقاء عودة OAuth العليا صالحة. |
| الصلاحيات | فصل viewer وanalyst وadmin في الخادم | لا تُعتمد البيانات ولا تُدار الأدوار من الواجهة فقط. |
| حوكمة القياسات | draft → reviewed → approved | يمنع المراجع الذاتي، ويقصر الاعتماد على المسؤول، ويمنع تجاوز مرحلة المراجعة. |
| الإدخال والاستيراد | Zod، حدود حجم وصفوف، تحقق وحدة/سنة/فترة/مصدر | تقليل الإدخال غير المتوافق ومنع استبدال قياس معتمد أو قياس لمستخدم آخر. |
| التنبؤ والنشر | قراءة القياسات المعتمدة فقط | لا تتحول التوقعات إلى قيم فعلية، ولا يُفعل نشر خارجي بلا تأكيد صريح. |

## التحصين المنفذ

أضيفت رؤوس حماية للخادم تشمل منع تفسير نوع محتوى غير متوقع، تقييد الكائنات المضمنة، تقييد ميزات المتصفح الحساسة، وضبط إحالة المصدر. كما خُفّض حد جسم الطلب إلى 8MB مع حد لعدد حقول النماذج، وأضيف حد زمني للطلبات تحت `/api` مع رؤوس توضح المتبقي ووقت إعادة الضبط. لا تستخدم المنصة CORS مفتوحاً، وتعطيل `X-Powered-By` يقلل كشف تفاصيل الخادم.

سُدت فجوة في القياسات الوطنية: أصبح تعديل قياس قائم مسموحاً لمسودة تخص مُدخلها فقط، وأصبحت الانتقالات تخضع للمراجعة المستقلة ثم اعتماد المسؤول كما هو مطبق للقياسات المكانية. كما أضيفت اختبارات تمنع اعتماد المحلل وتمنع استبدال قياس معتمد أو منسوب إلى مستخدم آخر.

تم تحديث tRPC وExpress وDrizzle وAWS SDK وAxios وJOSE وNanoid وStreamdown، ونقل `xlsx` إلى تبعيات التطوير لأنه يُستخدم في أدوات استخراج داخلية فقط ولا يحتاجه تشغيل المنصة. أظهر تدقيق التبعيات الإنتاجية النهائي **صفر مخاطر حرجة، وصفر مخاطر عالية، وخطر متوسط واحد**.

## التحقق المنجز

| تحقق | النتيجة |
|---|---|
| فحص الأنواع | ناجح بلا أخطاء TypeScript |
| الاختبارات | 99 اختباراً ناجحاً في 41 ملف اختبار |
| رؤوس الحماية الفعلية | ظهرت على نقطة API عامة مع حد الطلبات |
| الحوكمة الوطنية | اختبارات للمراجعة المستقلة والاعتماد ومنع الاستبدال غير المصرح ناجحة |
| تدقيق تبعيات الإنتاج | 0 حرج، 0 عالٍ، 1 متوسط |

## المخاطر المتبقية والمتابعة

يبقى تنبيه متوسط واحد في تبعية `uuid` المتداخلة المستخدمة عبر مكتبة توليد ملفات Excel. ينطبق التنبيه على استدعاءات اختيارية لممرر buffer في مولدات UUID v3/v5/v6؛ لا تستدعي المنصة هذا المسار ولا تقبل buffer من المستخدم في التصدير. يبقى هذا التنبيه تحت المراقبة إلى أن تصدر التبعية العليا تحديثاً متوافقاً يرفع UUID من دون كسر صادرات Excel.

تظل حماية الاستضافة وإدارة DNS وشهادات TLS وأسرار البيئة وعزل قاعدة البيانات من مسؤولية إعدادات المنصة والاستضافة. قبل النشر العام، ينبغي تدوير الأسرار عند الاشتباه، إبقاء الوصول الإداري محصوراً، ومراجعة سجل النشاط وحالة تدقيق التبعيات دورياً.
