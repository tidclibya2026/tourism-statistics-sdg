# دليل بيئة الاختبار ومراجعة التبعيات

## الغرض والنطاق

ينظم هذا الدليل تشغيل **بيئة اختبار مستقلة فعلياً** للمرصد الوطني للإحصاءات والمؤشرات السياحية قبل أي ترقية للبيئة الحية. وهو يغطي التحقق من التبعيات، واختبار التغييرات، والموافقة اليدوية، والرجوع إلى نقطة استعادة. لا يخول هذا الدليل أي شخص بتحديث مكتبات المنصة أو نشرها تلقائياً.

> **قاعدة ملزمة:** تقرير التبعيات هو رصد للأخطار فقط. لا يستدعي المسار الداخلي `update` أو `audit fix` أو أي تثبيت للحزم، ولا ينفذ ترقية إلى الحي. توضح وثائق npm أن `audit fix` يطبق معالجات على شجرة الحزم؛ لذلك يبقى خارج هذا المسار الآلي.[1]

## عقد الفصل بين البيئتين

| العنصر | بيئة الاختبار | البيئة الحية | الضابط المطلوب |
|---|---|---|---|
| النشر | مشروع أو نشر مستقل باسم واضح مثل `tourism-statistics-staging` | المشروع الحي الحالي | لا يعاد استخدام عنوان الحي للاختبار. |
| قاعدة البيانات | `DATABASE_URL` مستقل وقاعدة منفصلة | قاعدة بيانات الحي | يمنع اتصال الاختبار بقاعدة الحي، ولا تنسخ بيانات حقيقية إلا بلقطة مصرح بها ومخفاة الهوية. |
| OAuth ومعرّف التطبيق | `VITE_APP_ID` وإعداد OAuth منفصلان | إعدادات الحي | لا يعاد استخدام جلسات أو معرف تطبيق الحي. |
| أسرار الجلسات والخدمات | `JWT_SECRET` وأي مفاتيح مخصصة مستقلة | أسرار الحي | تحفظ في إعدادات المشروع فقط؛ لا تكتب في المحادثة أو المستودع. |
| وسم البيئة | `TOURISM_DEPLOYMENT_ENV=staging` | `TOURISM_DEPLOYMENT_ENV=production` | ترفض خدمة مراجعة التبعيات العمل إن لم يكن الوسم `staging`. |
| البيانات المعروضة | بيانات اختبار أو بيانات معتمدة ومخفاة الهوية عند الضرورة | القياسات المعتمدة فقط | لا تتحول بيانات اختبار إلى قيم منشورة. |

## مراجعة التبعيات داخل المنصة

تقرأ الخدمة الحزم الإنتاجية المثبتة فعلياً فقط، وتطلب من سجل npm تنبيهات Bulk Advisory. المسار الرسمي هو `/-/npm/v1/security/advisories/bulk`؛ وتوضح وثائق npm أن الاستجابة تحتوي تنبيهات مطابقة يمكن استخدامها لحساب مستوى الأثر.[1] لا تحفظ المنصة جسم الاستجابة أو مفاتيح البيئة؛ بل تحفظ أعداد الشدة وملخصاً موجزاً وحالة نجاح أو فشل فقط.

| خطوة | المسؤول | الناتج | ما لا يحدث |
|---|---|---|---|
| تشغيل «تقرير آمن» | عضو `admin` مفوض بقدرة `canReviewSecurity` | سجل مراجعة يدوي داخل قاعدة الاختبار | لا تثبيت ولا تغيير lockfile ولا نشر. |
| تشغيل دوري | مهمة موثوقة في بيئة الاختبار فقط | سجل مراجعة مجدول مع حراسة تكرار 10 دقائق | لا تقبل البيانات من body ولا تعمل على الحي. |
| فرز التنبيه | مسؤول الأمن ومالك المنصة | قرار: لا إجراء، أو اقتراح ترقية | لا يطبق الاقتراح تلقائياً. |
| تجربة الترقية | فريق مخول في فرع/نسخة اختبار | نتائج `pnpm check` و`pnpm test` وفحص وظيفي | لا تتغير حزم الحي. |
| ترقية الحي | مالك المنصة فقط بعد موافقة بشرية صريحة | نقطة استعادة جديدة ثم نشر يدوي | لا يجوز الترقية اعتماداً على تقرير وحده. |

## تشغيل المهمة الدورية بعد تجهيز الاختبار

لا تنشأ المهمة الدورية قبل نشر بيئة الاختبار، لأن عنوان التطوير المحلي غير قابل للوصول إلى خدمة الجدولة. بعد إنشاء النشر المستقل والتحقق من قاعدة البيانات والأسرار، ينشئ المالك المهمة باسم فريد وبمسار `/api/scheduled/dependency-review`. يستخدم المعالج مصادقة المهمة الموثوقة، ويتحقق من `isCron` و`taskUid`، ثم يبحث عن صف `dependencyReviewSchedules` بواسطة المعرّف الموثوق فقط. لا يثق في أي حقل من جسم الطلب.

يجب أن يحمل صف الإعداد في **قاعدة الاختبار فقط** القيم التالية قبل التفعيل: `environment=staging`، و`enabled=1`، و`scheduleCronTaskUid` المطابق لمعرف المهمة الذي أُنشئ بعد النشر. إن لم يجد المعالج الصف، يعيد نتيجة `orphan` ناجحة ولا ينفذ شيئاً. وعند إعادة محاولة منصة الجدولة للطلب، تمنع نافذة مطالبة مدتها عشر دقائق إنشاء تقرير مكرر.

## بوابة الترقية اليدوية

لا يرقى أي تغيير إلى الحي إلا بعد اجتياز جميع الشروط التالية. يشمل ذلك التحديثات الأمنية الصغيرة والكبيرة على حد سواء.

| شرط البوابة | دليل الإثبات | صاحب القرار |
|---|---|---|
| تقرير أمني واضح | آخر سجل مراجعة في الاختبار مع شرح مستوى الشدة | مسؤول الأمن المفوض |
| اقتراح محدد للتغيير | قائمة الحزم والإصدارات وأثرها المتوقع وخطة التراجع | فريق التطوير |
| اختبار تقني | `pnpm check` و`pnpm test` ناجحان في الاختبار | فريق التطوير |
| اختبار تشغيلي | تسجيل الدخول، لوحة البيانات، الاستيراد، الحوكمة، والواجهات العامة تعمل في الاختبار | المراجع المعين |
| موافقة النشر | موافقة صريحة من مالك المنصة | مالك المنصة |
| قابلية التراجع | نقطة استعادة محفوظة قبل النشر | مالك المنصة |

إذا فشل أي شرط، يبقى التغيير في الاختبار أو يعاد إلى الفرع السابق. لا يعالج تقرير التبعيات الفاشل بتجاوز الضوابط أو بتثبيت حزم عشوائياً.

## اختبار الدخان وقابلية الرجوع

ينفذ اختبار الدخان في بيئة الاختبار بعد كل ترقية مقترحة. يجب التحقق من تسجيل الدخول، سلامة الجلسة، تقييد صفحات المستخدمين والأمن، تدفق القياسات `draft → reviewed → approved`، عرض القياسات المعتمدة للزوار، وعدم تحويل Visit Libya أو أطلس ليبيا إلى تكامل خارجي بلا عقد وموافقة. بعد النشر الحي المعتمد، تراقب السجلات والواجهة لفترة تشغيلية متفق عليها. وعند حدوث أثر غير مقبول، تستخدم نقطة الاستعادة المحفوظة للرجوع إلى الإصدار السابق؛ لا تستخدم أوامر إعادة ضبط مدمرة للمستودع.

## حدود معروفة

التقرير الداخلي يراجع الحزم الإنتاجية المباشرة المثبتة، لا الشجرة الكاملة للـ lockfile ولا كل التبعيات المتداخلة. التدقيق الكامل للـ lockfile ينبغي أن ينفذ في خط تكامل مستمر أو بيئة اختبار متصلة بالمصدر، مع حفظ نتيجته في سجل التغيير. كما أن مهمة الجدولة **لن تُفعّل الآن**؛ فهي تعتمد على نشر بيئة اختبار مستقلة وقاعدة بيانات وأسرار ومعرّف تطبيق منفصلين.

## المراجع

[1]: https://docs.npmjs.com/cli/v10/commands/npm-audit "npm audit — npm Docs"
