# الدليل الكامل لمنصة الإحصائيات والمؤشرات السياحية

**الجهة المالكة:** مركز المعلومات والتوثيق السياحي

**نطاق الاستخدام:** داخلي ومقيد بالمستخدمين المصرح لهم.

## 1. كيف تعمل المنصة

المنصة مصدر وطني موحد للبيانات والإحصاءات والمؤشرات السياحية. تمر كل قيمة من تعريف المؤشر والمصدر، إلى الإدخال أو الاستيراد، ثم التحقق، والمراجعة المستقلة، والاعتماد، وبعدها تظهر في اللوحات والتقارير والخرائط والمساعد الذكي. لا تُعامل المسودة أو القياس المراجع أو التنبؤ على أنها رقم رسمي منشور.

> **قاعدة المصدر الواحد:** لا يدخل الرقم في المخرجات الرسمية إلا بعد وجود مصدر موثق، ووحدة وفترة واضحتين، ومرور السجل بالمراجعة والاعتماد.

## 2. المعمارية الكاملة

| الطبقة | التقنية أو الملفات | الوظيفة |
|---|---|---|
| المصادقة | Manus OAuth و`server/_core/oauth.ts` | تسجيل الدخول وبناء جلسة المستخدم. |
| الواجهة | React 19 وTypeScript وTailwind 4 | شاشات RTL والرسوم والفلاتر والتصدير. |
| التخطيط | `DashboardLayout` | القائمة الجانبية والحماية والجولة والثيم. |
| العقد | tRPC 11 و`client/src/lib/trpc.ts` | ربط typed بين الواجهة والخادم. |
| الخادم | Express و`server/routers.ts` | الإجراءات، التحقق، الصلاحيات ومسار الأعمال. |
| البيانات | Drizzle وMySQL/TiDB | الجداول والعلاقات والاستعلامات. |
| التخزين | S3 عبر `storage.ts` | المرفقات والملفات الكبيرة. |
| التحليل | Recharts وطبقات التنبؤ | الرسوم والمقارنات والسلاسل التقديرية. |
| المكان | Google Maps وطبقة البلديات الرسمية | البحث والتدرج الحراري ولوحة التفاصيل. |
| الذكاء | سياق بيانات مقيد في `server/dataAssistant.ts` | إجابات مبنية على السجلات المعتمدة فقط. |

## 3. الهيكل التنظيمي

يتولى المدخل أو المحلل إنشاء المسودة وتصحيح أخطاء الإدخال. يراجع محلل مستقل السجل ومصدره، ولا يراجع الشخص السجل الذي أدخله بنفسه. يعتمد رئيس الإحصاء السجلات المراجعة، بينما يتحقق مسؤول الأمن من الأدوار والصلاحيات والتبعيات والنسخ الاحتياطية. يدير مالك المنصة التهيئة العليا والقدرات الحساسة.

## 4. دورة المراجعة المستقلة والاعتماد خطوة بخطوة

### الخطوة الأولى: إعداد المؤشر

يُعرّف المؤشر بكود فريد واسم ووصف ومحور ووحدة وإطار مرجعي ومصدر وطريقة حساب. يجب أن تكون طريقة الحساب قابلة لإعادة الإنتاج، وأن يذكر المؤشر إن كان عدداً أو نسبة أو معدلاً أو قيمة مالية.

### الخطوة الثانية: إدخال القياس

يختار المدخل المؤشر والسنة والفترة والربع، ثم يدخل القيمة والمصدر والملاحظات والقيمة المستهدفة عند الحاجة. للبيانات المكانية يختار المدينة أو البلدية والرمز الصحيح. يحفظ النظام السجل كـ`draft`.

### الخطوة الثالثة: التحقق الآلي

يفحص الخادم النوع والقيمة والوحدة والفترة والربع والتكرار والحدود المسموح بها. لا يتوافق الربع مع الفترة السنوية، ولا تُقبل قيمة نصية في حقل رقمي، ولا يُسمح بتكرار السجل نفسه.

### الخطوة الرابعة: فحص المدخل

يطابق المدخل السجل مع المصدر، ويفحص السنة والربع والوحدة والمكان، ويضيف ملاحظة واضحة عند وجود مشكلة. لا يتجاوز المدخل خطأ المصدر بتعديل صامت.

### الخطوة الخامسة: المراجعة المستقلة

يفتح محلل آخر السجل والمصدر، ويتحقق من تعريف المؤشر، والوحدة، والفترة، والقيمة، والمكان، وطريقة التجميع. إذا كانت النتيجة صحيحة يغيّر الحالة إلى `reviewed`. إذا كانت غير صحيحة يرفض السجل مع سبب تصحيحي.

### الخطوة السادسة: اعتماد رئيس الإحصاء

يفتح رئيس الإحصاء قائمة السجلات `reviewed`، ويتحقق من هوية المدخل والمراجع، ووجود المصدر، وسلامة الوحدة والفترة، ثم يعتمد السجل إلى `approved`. لا يعتمد رئيس الإحصاء مسودة مباشرة ولا قياساً راجعه المدخل نفسه.

### الخطوة السابعة: ظهور المخرج

بعد الاعتماد يدخل السجل في لوحة المؤشرات والتقارير والخرائط وسياق المساعد. يبقى التنبؤ منفصلاً وموسوماً كتقديري، ولا يغيّر القياس الرسمي.

## 5. شرح الشاشات

| الشاشة | ما الذي يفعله المستخدم؟ | المخرج |
|---|---|---|
| لوحة المؤشرات `/` | يقرأ الملخص، يحدد السنة والمحور والمنطقة، يقارن المؤشرات ويراجع النشاط. | صورة وطنية ومكانية سريعة. |
| إدارة المؤشرات `/indicators` | ينشئ المؤشرات ويضبط الوحدة والمحور والمصدر والمنهجية. | قاموس مؤشرات صالح للاستخدام. |
| إدخال البيانات `/data` | ينشئ قياسات وطنية أو مكانية كمسودات. | سجلات جاهزة للتحقق. |
| التنبؤ `/forecast` | يختار سلسلة سنوية معتمدة ويعرض النمو والتوقع. | تحليل تقديري منفصل. |
| الأرشيف `/archive` | يراجع تاريخ السلاسل والقياسات السابقة. | سياق زمني طويل. |
| المدن السياحية `/spatial` | يبحث في المدن والبلديات ويفتح التفاصيل. | تحليل مكاني. |
| إدارة البيانات المكانية `/spatial-management` | يدير القياسات المكانية للمستخدمين المفوضين. | قياسات مكانية قابلة للمراجعة. |
| المقارنة `/compare` | يقارن مؤشرين أو مدينتين أو سنوات. | فرق ونسبة تغير وترتيب. |
| استيراد البيانات `/imports` | يرفع قالب Excel ويراجع أخطاء الدفعة. | مسودات مستوردة مع سجل مشكلات. |
| التقارير `/reports` | يطبق الفلاتر ويصدر PDF وExcel ويستدعي الملخص الذكي. | تقارير قابلة للمشاركة. |
| مركز النشر `/publication` | يراجع حزم Visit Libya وأطلس ليبيا ضمن التفويض. | وجهة نشر داخلية أو مؤجلة. |
| المستخدمون `/users` | يدير الأعضاء والقدرات عند امتلاك الصلاحية. | مصفوفة صلاحيات قابلة للتدقيق. |
| مراجعة الأمان `/security` | يراجع الأمن والتبعيات وسجل الجاهزية. | قرارات أمنية موثقة. |
| إدارة الدعم `/support-admin` | يراجع التذاكر والردود والتقييمات. | دورة دعم قابلة للمتابعة. |
| الملف الشخصي `/profile` | يضبط التفضيلات والثيم واللغة وإعدادات العرض. | تجربة شخصية محفوظة. |
| المساعدة `/help` | يقرأ الدليل ويستخدم مساعد البيانات ويرسل تذكرة. | معرفة تشغيلية ودعم. |
| مكتبة الوثائق `/documents` | يتصفح الأدلة وينزلها أو يجمعها ZIP عند التفويض. | حزمة توثيق رسمية. |

## 6. قاعدة البيانات

تتكون قاعدة البيانات من مجموعات الهوية والصلاحيات، المؤشرات، القياسات الوطنية، القياسات المكانية، الاستيراد، الدعم، النشر، مراجعة التبعيات، والتفضيلات. ترتبط القياسات بالمؤشرات، وترتبط القياسات المكانية بالمؤشر والمكان، وتحافظ سجلات المراجعة على أثر الحالة والمراجع. تحمي قيود الحذف السجل المرجعي من الحذف عندما توجد قياسات مرتبطة.

الجداول التفصيلية موثقة في [`platform_architecture_and_data_model_ar.md`](./platform_architecture_and_data_model_ar.md)، ولا تُجرى تغييرات مخطط القاعدة إلا عبر مخطط Drizzle وترحيل SQL والتحقق اللاحق.

## 7. كود واجهة مكتبة الوثائق

الكود التالي يوضح البنية المقترحة للصفحة. الوصول إليها يجب أن يكون خلف `DashboardLayout`، ويظهر زر ZIP للمستخدم الإداري المصرح له فقط.

```tsx
import { useMemo, useState } from "react";
import { zipSync, strToU8 } from "fflate";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Card } from "@/components/ui/card";
import { Download, FileText, Package, Search } from "lucide-react";
import { documentLibrary } from "@/lib/documentLibrary";

export default function Documents() {
  const [query, setQuery] = useState("");
  const filtered = useMemo(() => documentLibrary.filter((doc) =>
    `${doc.title} ${doc.description} ${doc.category}`.toLowerCase().includes(query.toLowerCase())
  ), [query]);

  const download = (name: string, content: string) => {
    const blob = new Blob([content], { type: "text/markdown;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const link = document.createElement("a");
    link.href = url; link.download = name; link.click(); URL.revokeObjectURL(url);
  };

  const downloadZip = () => {
    const files = Object.fromEntries(documentLibrary.map((doc) => [doc.fileName, strToU8(doc.content)]));
    const blob = new Blob([zipSync(files, { level: 6 })], { type: "application/zip" });
    const url = URL.createObjectURL(blob);
    const link = document.createElement("a");
    link.href = url; link.download = `tidc-documentation-${new Date().toISOString().slice(0, 10)}.zip`;
    link.click(); URL.revokeObjectURL(url);
  };

  return <main dir="rtl" className="space-y-6">
    <header className="rounded-3xl bg-[#0f5c58] p-7 text-white">
      <p className="text-xs text-amber-200">مركز المعرفة المؤسسي</p>
      <h1 className="mt-2 text-3xl font-bold">مكتبة الوثائق</h1>
      <p className="mt-2 text-teal-50/80">أدلة الأمن والإحصاء والتشغيل والمعمارية.</p>
      <div className="mt-5 flex flex-wrap gap-2">
        <Button onClick={downloadZip} className="bg-[#d9a357] text-[#173f3d] hover:bg-[#edb96d]"><Package className="ml-2 h-4 w-4" />تصدير حزمة ZIP</Button>
        <div className="relative"><Search className="pointer-events-none absolute right-3 top-3 h-4 w-4" /><Input value={query} onChange={(e) => setQuery(e.target.value)} className="pr-9" placeholder="ابحث في الأدلة…" /></div>
      </div>
    </header>
    <section className="grid gap-4 md:grid-cols-2">{filtered.map((doc) => <Card key={doc.fileName} className="p-5"><FileText className="h-5 w-5 text-[#0f766e]" /><h2 className="mt-3 font-bold">{doc.title}</h2><p className="mt-2 text-sm text-slate-600">{doc.description}</p><Button variant="outline" className="mt-4" onClick={() => download(doc.fileName, doc.content)}><Download className="ml-2 h-4 w-4" />تنزيل الدليل</Button></Card>)}</section>
  </main>;
}
```

## 8. تفاصيل حزمة ZIP

تتكون الحزمة المقترحة من مجلدات ثابتة حتى يسهل تداولها داخل المركز:

| المجلد | الملفات |
|---|---|
| `01-architecture` | المعمارية ونموذج البيانات وتقرير التشطيب. |
| `02-security` | دليل مسؤول الأمن وتقرير الأمان والسياسة الداخلية. |
| `03-statistics` | دليل رئيس الإحصاء وتعريف المؤشرات والاعتماد. |
| `04-operations` | الدليل التشغيلي ودليل النسخ الاحتياطي والتطوير. |
| `05-governance` | سجل مصدر طبقة البلديات وسياسة النشر الداخلي. |

اسم الملف يتضمن التاريخ ورقم الإصدار. لا يجب أن تحتوي الحزمة على الأسرار أو `DATABASE_URL` أو سجلات المستخدمين أو ملفات قاعدة البيانات أو بيانات شخصية. زر ZIP يجب أن يُحجب عن القارئ، ويمكن قصره على `admin` ذي القدرة الإدارية المناسبة.

## 9. الجولة التعريفية

تبدأ الجولة عند أول دخول أو من زر «جولة». تعرض للمستخدم شاشة لوحة المؤشرات، ثم إدخال المؤشر، ثم القياس كمسودة، ثم المراجعة المستقلة، ثم اعتماد رئيس الإحصاء، ثم التنبؤ، والتقارير، والخريطة، والمساعد، ومكتبة الوثائق. تختلف الرسائل حسب الدور؛ فالمدخل يرى التركيز على الإدخال، والمراجع على التحقق، ورئيس الإحصاء على الاعتماد، ومسؤول الأمن على الصلاحيات وسجل التدقيق. يحفظ النظام التقدم محلياً، ويمكن الإنهاء أو التأجيل.

## 10. الضوابط النهائية

لا تُنشر قيمة بلا `approved`. لا يراجع المدخل قياسه بنفسه. لا يُعتبر التنبؤ رقماً رسمياً. لا تُرفع الأسرار إلى GitHub. لا تُفتح وجهات خارجية دون بيئة اختبار وأسرار منفصلة واختبار استعادة واختبار أمني. جميع المخرجات يجب أن تذكر النطاق والفترة والمصدر والحالة عند الحاجة.

## 11. مراجع المشروع

- [`platform_architecture_and_data_model_ar.md`](./platform_architecture_and_data_model_ar.md)
- [`security_officer_guide_ar.md`](./security_officer_guide_ar.md)
- [`statistics_chief_user_guide_ar.md`](./statistics_chief_user_guide_ar.md)
- [`end_to_end_operating_manual_ar.md`](./end_to_end_operating_manual_ar.md)
- [`institutional_completion_report_ar.md`](./institutional_completion_report_ar.md)
- [`deployment-backup-development-guide-ar.md`](./deployment-backup-development-guide-ar.md)
- [`internal_use_and_publication_policy.md`](./internal_use_and_publication_policy.md)
