# تقرير تدقيق التبعيات والأمن — 26 أغسطس 2026

## نطاق المراجعة

أجري تدقيقان **تقريريان فقط** على شجرة الحزم المقفلة في النسخة المحلية المطابقة لفرع `main` في GitHub. لم يُشغّل `pnpm update` أو `pnpm audit fix` ولم تُعدّل أي حزمة تلقائياً. شمل التحقق أيضاً فحص TypeScript والاختبارات الآلية المتاحة.

| الفحص | النتيجة |
|---|---:|
| `pnpm audit --json` (كل التبعيات، بما فيها التطوير) | 2 حرجة، 29 عالية، 28 متوسطة، 1 منخفضة |
| `pnpm audit --prod --json` (تبعيات الإنتاج فقط) | 0 حرجة، 0 عالية، 1 متوسطة، 0 منخفضة |
| `pnpm check` | ناجح |
| اختبارات Vitest بعد توسعة التدقيق | 131 اختباراً ناجحاً في 53 ملف اختبار |

## الأولويات

| الأولوية | الحزمة والمسار | الأثر | الإجراء المقترح |
|---|---|---|---|
| حرجة | `vitest@2.1.9`، تبعية تطوير مباشرة | يقتصر على بيئة الاختبار، لكنه يتطلب ترقية قبل تشغيل اختبارات غير موثوقة أو CI. | ترقية مدروسة إلى `vitest >= 3.2.6` في بيئة اختبار، ثم تشغيل `pnpm check && pnpm test`. [1] |
| حرجة | `tar@7.5.1` عبر `@tailwindcss/vite → @tailwindcss/oxide` | تبعية تطوير، لا يشير تدقيق الإنتاج إلى وصولها وقت تشغيل الخادم. | ترقية Tailwind/Vite ومراجعة تغييرات التوافق في بيئة اختبار، ثم إعادة التدقيق. [2] |
| عالية | `xlsx@0.18.5` | تستخدم الواجهة تحليل ملفات مستوردة؛ لذلك لا يجوز معالجة ملفات مجهولة خارج القيود الحالية. | تقييم استبدال مكتبة SheetJS أو عزل التحليل في خدمة مقيدة بعد اختبار الاستيراد. لا يوجد تحديث آلي مقترح من هذا التقرير. [3] [4] |
| متوسطة في الإنتاج | `uuid@8.3.2` عبر `exceljs@4.4.0` | يؤثر في شجرة الإنتاج وفق تقرير `--prod`. | اختبار ترقية `exceljs` أو معالجة متوافقة في بيئة اختبار، ثم تدقيق الإنتاج مرة أخرى. [5] |

## قرار النشر

لا يكفي نجاح الاختبارات وحده لتحويل التنبيهات الحرجة في تبعيات التطوير إلى خطر صفري. ومع ذلك، لم يظهر تدقيق الإنتاج أي تنبيه حرج أو عالٍ في هذه المراجعة. قبل نشر بيئة حية أو ترقية نسخة حالية، يلزم إجراء ترقية يدوية معتمدة للحزم ذات الأولوية وتشغيل الاختبارات في بيئة اختبار مستقلة. لا يطبق هذا التقرير تحديثات على البيئة الحية.

## خطة معالجة يدوية

1. أنشئ نسخة اختبار بقاعدة وأسرار وOAuth مستقلة، واضبط `TOURISM_DEPLOYMENT_ENV=staging`.
2. أنشئ فرعاً خاصاً للترقيات، وحدث حزمة واحدة أو مجموعة متوافقة في كل مرة.
3. افحص الترحيلات وملف القفل، ثم شغّل `pnpm check && pnpm test && pnpm audit --prod --json`.
4. اختبر استيراد Excel وCSV ومرفقات الدعم وتصديرات Excel/PDF وتدفقات OAuth يدوياً في الاختبار.
5. راجع فرق الحزم ونتائج التدقيق مع مسؤول مفوض، ثم اعتمد ترقية يدوية فقط إذا اجتازت الاختبارات.
6. احفظ نقطة استعادة قبل الترقية إلى الحي، واحتفظ بخيار رجوع واضح.

## التغطية الآلية المضافة

توسعت اختبارات واجهة لوحة التحكم للتحقق من بطاقات القياسات المعتمدة، وتبديل فلتر المحور، ورسم تحقيق المستهدفات. كما توسعت اختبارات المساعد الذكي للتحقق من اقتصار سجل المحادثة على آخر ست رسائل ومن عودته برسالة آمنة توجه إلى الدعم عند غياب استجابة النموذج. نجحت `pnpm check` و`pnpm test` بعد هذه الإضافة.

## ملاحظات تشغيلية

ظهر تحذير من pnpm بأن حقلي `patchedDependencies` و`overrides` داخل `package.json` لم يعودا موضعهما المفضل. لا تنقل هذه الإعدادات إلى `pnpm-workspace.yaml` من دون اختبار؛ لأن المشروع يعتمد patch لـ Wouter وoverride لـ Nanoid ضمن Tailwind.

## المراجع

[1] [GitHub Advisory: Vitest path traversal](https://github.com/advisories/GHSA-5xrq-8626-4rwp)

[2] [GitHub Advisory: tar denial of service](https://github.com/advisories/GHSA-23hp-3jrh-7fpw)

[3] [GitHub Advisory: SheetJS prototype pollution](https://github.com/advisories/GHSA-4r6h-8v6p-xvw6)

[4] [GitHub Advisory: SheetJS ReDoS](https://github.com/advisories/GHSA-5pgg-2g8v-p4x9)

[5] [GitHub Advisory: uuid buffer bounds check](https://github.com/advisories/GHSA-w5hq-g745-h8pq)
