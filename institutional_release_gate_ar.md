# بوابة الجاهزية المؤسسية للإصدار

تمنع هذه البوابة دمج إصدار غير موثّق في `main`. وهي لا تنشر إلى الإنتاج تلقائياً ولا تستبدل موافقة المسؤول المؤسسي.

## الحواجز الآلية

1. فحص الملفات المتتبعة بحثاً عن مفاتيح وأسرار شائعة، من دون طباعة قيمها.
2. التحقق من تسلسل ترحيلات قاعدة البيانات.
3. فحص TypeScript وتشغيل جميع الاختبارات.
4. تدقيق تبعيات الإنتاج ذات الخطورة العالية أو الحرجة.
5. بناء التطبيق للإنتاج.
6. بناء صورة التشغيل وفحصها بواسطة Trivy بحثاً عن ثغرات `HIGH` و`CRITICAL` القابلة للإصلاح.

لتشغيل البوابة محلياً من شجرة عمل نظيفة:

```powershell
pnpm release:gate
```

أثناء التطوير فقط، يمكن السماح بشجرة عمل غير نظيفة:

```powershell
pnpm release:gate:dev
```

ينتج الأمر `release-readiness-report.json`. يحتوي التقرير أسماء الحواجز وحالتها ومدتها فقط، ولا يحفظ مخرجات الأوامر أو متغيرات البيئة أو الأسرار.

## أدلة CI

عند تشغيل GitHub Actions ينتج أثران محفوظان لمدة 30 يوماً:

- `release-evidence-quality-<commit>` لجودة الشفرة والاختبارات والتدقيق والبناء.
- `release-evidence-container-<commit>` لبناء صورة التشغيل وفحص الثغرات.

نجاح الأثر يعني أن كل الحواجز المسماة فيه نجحت للـ commit نفسه. لا يُنشأ الأثر إذا فشل حاجز سابق.

## قرار الدمج

لا يُدمج فرع الجاهزية في `main` إلا بعد تحقق جميع الشروط التالية:

- نجاح وظيفتي `quality` و`container` في CI للـ commit المرشح.
- اكتمال [قائمة القبول الشامل في بيئة Staging](staging_full_acceptance_checklist.md) وتوثيق نتائجها.
- مراجعة تقارير الأدلة وربطها بطلب السحب.
- موافقة المسؤول التقني والمسؤول المؤسسي المخوّل.
- وجود خطة تراجع ونسخة احتياطية تم التحقق منها وفق دليل النشر والنسخ الاحتياطي.

أي فشل أو دليل ناقص يعيد القرار إلى «غير جاهز» حتى المعالجة وإعادة التشغيل على commit جديد.
