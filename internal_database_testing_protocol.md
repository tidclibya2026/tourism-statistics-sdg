# بروتوكول الاختبار الداخلي لقاعدة البيانات والمؤشرات

## الغرض

يحدد هذا البروتوكول طريقة استخدام قاعدة البيانات الداخلية لمنصة المرصد من قبل **قسم الإحصاء** و**المدير العام** في اختبار قراءة البيانات، وتحليل الإحصاءات، وقياس المؤشرات. لا يحول البروتوكول المنصة إلى خدمة عامة، ولا يغير قرار بقائها محصورة داخل مركز المعلومات والتوثيق السياحي.

## قاعدة الحماية الأساسية

> القياس ذو الحالة `approved` سجل رسمي للعرض والتقارير، ويُستخدم في الاختبار والتحليل بوضع القراءة فقط. لا يجوز تعديله أو حذفه أو استبداله خلال تجارب القسم.

| نوع النشاط | المدير العام | قسم الإحصاء / المحلل | الضابط |
|---|---|---|---|
| قراءة اللوحات والرسوم والتقارير | مسموح | مسموح بحسب الصلاحية | تعرض القياسات المعتمدة فقط. |
| الفلاتر والمقارنة والتنبؤ والتحليل | مسموح | مسموح بحسب الصلاحية | لا تغير هذه العمليات قاعدة البيانات. |
| تنزيل Excel أو PDF | مسموح | مسموح بحسب الصلاحية | يقتصر على نتائج البيانات المعتمدة والفلاتر الحالية. |
| اختبار نموذج الإدخال وقواعد التحقق | مسموح بالمشاهدة | مسموح بالمشاهدة | لا يضغط المستخدم زر الحفظ ما لم يكن لديه مصدر رسمي كامل. |
| إنشاء مسودة | لا ينشئ المدير العام قياساً تجريبياً | مسموح للمحلل عند وجود مصدر رسمي فعلي | تبقى مسودة ولا تدخل اللوحات أو النشر. |
| مراجعة المسودات | حسب قدرة الاعتماد الممنوحة | مراجع مستقل عن المُدخل فقط | يمنع النظام المراجعة الذاتية. |
| اعتماد أو سحب قياس | مسؤول مفوض فقط | غير مسموح | لا يعتمد إلا سجل reviewed المكتمل المصدر. |
| تغيير المستخدمين أو الأسرار أو التبعيات | ممنوع ضمن اختبار البيانات | ممنوع ضمن اختبار البيانات | يتطلب مساراً إدارياً مستقلاً. |

## مسار الاختبار الموصى به

يبدأ المدير العام وقسم الإحصاء باختيار مؤشر وسنة من القياسات المعتمدة الموجودة فعلياً، ثم يستخدمان لوحة المؤشرات والتقارير والمقارنة والتنبؤ والخريطة—عند وجود قياس مدني—لفحص القيم والمصادر والفلاتر من دون إنشاء سجلات جديدة. توثق أي ملاحظة على القيمة أو المصدر في قائمة عمل القسم أو تذكرة الدعم، وليس بتغيير السجل الرسمي مباشرة.

عند توفر ملف رسمي جديد، يستخدم المحلل قالب الاستيراد أو نموذج الإدخال ويقدم المصدر والوحدة والسنة ورقم الجدول أو الصفحة. لا يُنشأ أي رقم لأغراض التجربة ولا تُستخدم أصفار بديلة. ينشأ السجل كمسودة فقط، ويظل خارج لوحات المدير العام والتقارير المعتمدة إلى أن يراجعه مستخدم مستقل ثم يعتمده مسؤول مفوض.

تطابق واجهة سجل القياسات هذه القاعدة: لا تعرض للمحلل خيار اعتماد القياس، ولا تعرض له مراجعة مسودته الشخصية. وتعرض للمسؤول خيارات الاعتماد أو الرفض أو إعادة المسودة فقط حين تسمح حالة القياس بذلك. تظل حراسة الخادم هي المرجع النهائي لهذه الصلاحيات.

## ما لا يختبر في قاعدة البيانات التشغيلية

لا تستخدم القاعدة الداخلية الحالية لتجربة قيم افتراضية أو تكرار قياسات معتمدة أو اختبار تغيير الأسرار أو OAuth أو ترقية التبعيات أو تشغيل مهمة دورية. هذه الأنشطة تؤجل إلى بيئة اختبار مستقلة حين يعتمد المركز إنشاءها بقاعدة بيانات وأسرار منفصلة.

## سجل تجربة القسم

لكل جلسة اختبار، يسجل قسم الإحصاء: التاريخ، اسم المستخدم، المؤشر أو المدينة، الفترة، نوع الإجراء، مصدر البيانات، والنتيجة. إذا نتجت ملاحظة تحتاج تصحيحاً، ترفق بالمصدر الرسمي وتُمرر عبر مسار المسودة والمراجعة والاعتماد؛ ولا تعدل القياسات المعتمدة مباشرة.
