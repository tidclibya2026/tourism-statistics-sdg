# دليل النسخ الاحتياطي والتطوير والانتقال السحابي

**المشروع:** منصة الإحصائيات والمؤشرات السياحية

**الغرض:** حماية نسخة المركز الداخلية، وتمكين فريق التطوير من إجراء التعديلات بأمان، ثم الانتقال مستقبلاً إلى استضافة مستقلة عندما يصدر القرار الإداري بذلك.

> هذا الدليل يفرّق بين ثلاثة أشياء: نسخة الكود، ونسخة قاعدة البيانات، ونسخة ملفات وإعدادات المنصة. حفظ الكود في GitHub وحده لا يساوي نسخة احتياطية كاملة.

## 1. الوضع الحالي للمنصة

الرابط الحالي هو بيئة التشغيل الداخلية:

`https://tourism-sdg-x4y32gsg.manus.space/`

المشروع يستخدم React وTypeScript في الواجهة، وExpress وtRPC في الخادم، وDrizzle ORM مع MySQL/TiDB لقاعدة البيانات، إضافة إلى Manus OAuth والتخزين الكائني للملفات. لذلك لا يصلح GitHub Pages لتشغيل المنصة الكاملة؛ فهو يستطيع عرض ملفات ثابتة فقط، ولا يوفر الخادم أو قاعدة البيانات أو OAuth أو الصلاحيات.

يجب إبقاء المستودع خاصاً، وعدم وضع كلمات المرور أو مفاتيح OAuth أو `DATABASE_URL` أو مفاتيح التخزين في GitHub.

## 2. ما الذي يجب نسخه احتياطياً؟

| المكوّن | ماذا يحتوي؟ | طريقة النسخ الصحيحة |
|---|---|---|
| الكود | `client/`, `server/`, `shared/`, `drizzle/`, ملفات الإعداد و`package.json` | GitHub خاص أو ملف ZIP محلي |
| قاعدة البيانات | المؤشرات والقياسات والمستخدمون وسجل التدقيق والإعدادات | SQL dump أو أداة تصدير قاعدة البيانات |
| ملفات التخزين | ملفات Excel وPDF والمرفقات والصور الموجودة في التخزين الكائني | تصدير التخزين/Task Data Backup أو تنزيل الملفات من مسار التخزين |
| الأسرار | OAuth وJWT وواجهات Manus وS3 | مدير أسرار آمن، وليس GitHub أو ملفاً مكشوفاً |
| إعدادات التشغيل | النطاقات والصلاحيات والوجهات والمهام المجدولة | توثيق مستقل ونسخة إعدادات، مع عدم مشاركة القيم السرية |
| نسخة Manus الكاملة | الكود والقاعدة والملفات والإعدادات المرتبطة بالمشروع | Task Data Backup من أداة النسخ الرسمية |

كل تصدير كامل من Manus هو **لقطة زمنية**؛ لا يتزامن تلقائياً مع التغييرات اللاحقة. وبحسب إرشادات Manus، لا تعتبر نسخة الكود بديلاً عن نسخة Task Data Backup، لأن نسخة الكود لا تتضمن قاعدة البيانات والملفات والإعدادات والقدرات المستضافة [1] [2].

## 3. إنشاء نسخة محلية من الكود

### الطريقة الأسهل عبر GitHub Desktop

افتح مستودع المشروع:

`https://github.com/tidclibya2026/tourism-statistics-sdg`

ثم اختر **Code**، وبعدها افتحه بواسطة GitHub Desktop أو انسخ رابط HTTPS. اختر مجلداً آمناً على جهاز المركز، ثم نفّذ الاستنساخ. الاستنساخ ينشئ نسخة كاملة من ملفات وإصدارات المستودع الموجودة وقت التنفيذ [3].

### الطريقة عبر الطرفية

بعد تثبيت Git أو GitHub CLI على جهاز المركز، استخدم:

```bash
gh repo clone tidclibya2026/tourism-statistics-sdg
cd tourism-statistics-sdg
```

أو باستخدام Git مباشرة:

```bash
git clone https://github.com/tidclibya2026/tourism-statistics-sdg.git
cd tourism-statistics-sdg
```

ثم ثبّت الاعتماديات المطابقة للقفل:

```bash
pnpm install --frozen-lockfile
```

لا تنسخ ملف `.env` إلى GitHub. أنشئ ملفاً محلياً خاصاً، أو استخدم مدير أسرار النظام، ولا تشاركه عبر البريد أو WhatsApp.

### إنشاء أرشيف محلي للكود

بعد التأكد من وجود النسخة:

```bash
git status
mkdir -p backups/code
zip -r backups/code/tourism-statistics-sdg-$(date +%F).zip . \\
  -x 'node_modules/*' 'dist/*' '.env' '.env.*' '.git/*'
```

احتفظ بنسختين على الأقل في مكانين مختلفين: قرص مشفّر داخل المركز ووسيط احتياطي منفصل. لا تجعل مجلد النسخ الاحتياطية متاحاً عبر رابط الويب.

## 4. إنشاء نسخة محلية من قاعدة البيانات

### أولاً: لا تستخدم قاعدة الإنتاج مباشرة للتجربة

أنشئ قاعدة اختبار منفصلة، أو نسخة محلية، ثم استعد إليها. لا تشغّل أوامر حذف أو ترحيل على قاعدة الإنتاج أثناء التعلم أو تطوير ميزة جديدة.

### إذا كان اتصال MySQL متاحاً من جهاز المركز

ثبّت عميل MySQL، ثم استخدم `mysqldump`. الأداة تنشئ أوامر `CREATE` و`INSERT` يمكن تحميلها لاحقاً إلى خادم MySQL آخر، ويمكن استخدامها أيضاً لعمل نسخة للتجارب [4].

لا تضع كلمة المرور في الأمر حتى لا تظهر في سجل الطرفية. استخدم مطالبة كلمة المرور:

```bash
mkdir -p backups/database
mysqldump \\
  --host=HOST \\
  --port=3306 \\
  --user=USER \\
  --single-transaction \\
  --routines \\
  --triggers \\
  --hex-blob \\
  DATABASE_NAME \\
  > backups/database/tourism-statistics-$(date +%F).sql
```

سيطلب البرنامج كلمة المرور بصورة تفاعلية. استبدل `HOST` و`USER` و`DATABASE_NAME` بالقيم التي يقدمها مسؤول الاستضافة. لا تستخدم قيمة `DATABASE_URL` الحقيقية في مستند أو رسالة عامة.

لضغط الملف بعد التحقق من وجوده:

```bash
gzip -9 backups/database/tourism-statistics-YYYY-MM-DD.sql
sha256sum backups/database/tourism-statistics-YYYY-MM-DD.sql.gz \\
  > backups/database/tourism-statistics-YYYY-MM-DD.sha256
```

تحقق آلياً من الملف والبصمة قبل نقله أو اعتماده للاستعادة:

```bash
pnpm db:backup:verify backups/database/tourism-statistics-YYYY-MM-DD.sql.gz SHA256_VALUE
```

لا تنفذ الاستعادة إذا فشل التحقق، أو كان الملف فارغاً، أو لم يكن من نوع `gzip`.

### استعادة النسخة إلى قاعدة اختبار

أنشئ قاعدة اختبار فارغة، ثم نفّذ الاستعادة إليها فقط:

```bash
gunzip -c backups/database/tourism-statistics-YYYY-MM-DD.sql.gz \\
  | mysql --host=TEST_HOST --port=3306 --user=TEST_USER --password TEST_DATABASE
```

بعد ذلك تحقق من عدد الجداول، وعدد المؤشرات، وعدد القياسات المعتمدة، وسجل التدقيق. يجب مقارنة هذه الأعداد مع سجل التصدير، دون تعديل السجلات الرسمية.

### إذا لم يسمح Manus باتصال MySQL مباشر

استخدم **لوحة Database** أو **Task Data Backup** من Manus. افتح أداة النسخ الرسمية واختر النسخة الخاصة بالمشروع/الموقع، ثم احفظ جميع أجزاء الحزمة كما هي. لا تعِد تسمية الحزم أو تخلط أجزاء من تصديرين مختلفين. تذكر أن الحزمة تشمل عناصر لا تحصل عليها من `mysqldump` وحده، مثل ملفات الموقع وإعداداته والملفات المرفوعة [1] [2].

## 5. قاعدة 3-2-1 للنسخ الاحتياطي

طبّق القاعدة التالية على النسخة الرسمية:

| القاعدة | التطبيق في المركز |
|---|---|
| 3 نسخ | النسخة الأصلية + نسختان احتياطيتان |
| وسيطان مختلفان | قرص داخلي مشفّر وقرص خارجي/تخزين سحابي مؤسسي |
| نسخة واحدة خارج الجهاز | مكان منفصل أو تخزين مؤسسي بصلاحيات مقيدة |
|

اختبر الاستعادة مرة دورياً على قاعدة اختبار. لا يكفي أن يكون الملف موجوداً؛ يجب التأكد من أنه قابل للقراءة والاستعادة. سجّل تاريخ النسخة، مصدرها، حجمها، قيمة SHA-256، والشخص الذي تحقق منها.

## 6. كيف تعدّل الكود وتضيف ميزة جديدة؟

### دورة العمل المقترحة

1. **اكتب وصفاً قصيراً للميزة.** حدّد المستخدم المستفيد، والبيانات التي تقرأها، وما إذا كانت الميزة تعدّل سجلات رسمية أو تنشئ مسودات.
2. **أنشئ فرعاً مستقلاً.** لا تعدّل الفرع الرئيسي مباشرة:

```bash
git checkout main
git pull origin main
git checkout -b feature/اسم-الميزة
```

3. **افحص المكونات الموجودة أولاً.** استخدم `DashboardLayout` للتدفقات الداخلية، ومكونات shadcn/ui، ولا تعِد بناء مكوّن موجود.
4. **إذا كانت الميزة بيانات جديدة، ابدأ بالنموذج.** عدّل `drizzle/schema.ts`، وحدّد العلاقة والحالة والصلاحيات قبل كتابة الواجهة.
5. **طبّق التغيير على قاعدة اختبار.** لا تستخدم قاعدة الإنتاج.
6. **أضف إجراء tRPC محمياً.** ضع التحقق من الدور والحالة والوحدة والمصدر في الخادم، وليس في الواجهة فقط.
7. **أضف الواجهة العربية RTL.** اربطها بـ`trpc.*.useQuery` و`useMutation`، وأضف حالات التحميل والفراغ والخطأ وإعادة المحاولة.
8. **أضف اختبار Vitest.** يجب اختبار المسار الناجح، والصلاحيات، والبيانات غير الصحيحة، وعدم تسريب المسودة إلى العرض الرسمي.
9. **شغّل الفحوصات قبل الدمج:**

```bash
pnpm check
pnpm test -- --run
pnpm build
```

10. **راجع التغيير بصرياً.** اختبر سطح المكتب والهاتف، وراجع اتجاه RTL، والسنوات مثل `2025` دون نقاط أو فواصل.
11. **افتح Pull Request.** راجع التغيير مع مسؤول آخر، ثم ادمجه بعد نجاح الاختبارات فقط.
12. **أنشئ نقطة تحقق قبل أي نشر.** احتفظ بسجل يوضح ما تغير، وما اختُبر، وما إذا كانت قاعدة البيانات قد تغيرت.

### عند إضافة جدول أو عمود جديد

المسار الآمن هو:

```text
schema.ts
   ↓
توليد migration
   ↓
قراءة SQL ومراجعته
   ↓
تطبيقه على قاعدة الاختبار
   ↓
اختبارات واستعادة تجريبية
   ↓
موافقة إدارية
   ↓
تطبيق مخطط مضبوط على البيئة الحية
```

في هذا المشروع لا تشغّل `pnpm db:push` عشوائياً على قاعدة البيانات الرسمية؛ فالبرنامج الحالي يولّد migration ثم ينفّذها. يجب مراجعة ملف SQL أولاً، ونسخ القاعدة قبل أي تغيير بنيوي، وعدم حذف أعمدة أو جداول رسمية دون خطة استعادة وموافقة.

قبل رفع أي تغيير في المخطط شغّل الفحص الساكن، وهو لا يتصل بأي قاعدة:

```bash
pnpm db:readiness
```

وعلى بيئة اختبار مضبوطة فقط يمكن لمسؤول التشغيل فحص الاتصال وTLS دون إظهار عنوان القاعدة أو كلمة المرور:

```bash
pnpm db:readiness:connect
```

### قاعدة الحوكمة للبيانات

أي بيانات يدخلها قسم الإحصاء تبدأ كمسودة. تمر بالمراجعة المستقلة، ثم اعتماد المسؤول المفوض. لا يجوز للمُدخل اعتماد قياسه بنفسه. التنبؤات إسقاطات تحليلية وليست قياسات رسمية، ويجب أن تبقى منفصلة في الواجهة والتقارير.

## 7. ما الذي لا يجب فعله؟

| لا تفعل | السبب |
|---|---|
| رفع `.env` أو أسرار OAuth إلى GitHub | يعرّض الحساب وقاعدة البيانات للاختراق |
| وضع كلمة مرور قاعدة البيانات داخل الكود | يصعب إلغاؤها وتظهر في سجل Git |
| اختبار ميزة جديدة على قاعدة الإنتاج | قد يغيّر القياسات الرسمية أو سجل التدقيق |
| استخدام GitHub Pages للمنصة الكاملة | لا يشغّل Express أو MySQL أو OAuth |
| إدخال أرقام تجريبية على أنها رسمية | يفسد المؤشرات والتقارير والقرارات |
| اعتماد قياس أدخله الشخص نفسه | يخالف الفصل بين الإدخال والمراجعة والاعتماد |
| تنفيذ تحديث تبعيات تلقائي على الحي | قد يكسر التشغيل أو يفتح ثغرة دون مراجعة |
| حذف السجلات المرفوضة | يجب حفظ قرار السحب وسجل التدقيق |

## 8. الخطة المستقبلية للنشر على استضافة سحابية حقيقية

لا تبدأ النقل قبل موافقة المدير العام واعتماد خطة الاستضافة. الخيار المفضل هو استضافة مُدارة لتطبيق Node.js مع قاعدة MySQL/TiDB متوافقة، وObject Storage للملفات، وإدارة أسرار، وشهادة TLS، ونسخ احتياطي مُدار.

### المرحلة الأولى: قرار الاستضافة

قارن بين الاستمرار على الاستضافة المُدارة الحالية، أو استضافة سحابية خارجية مُدارة، أو خادم مؤسسي خاص. الاستضافة المُدارة تقلل الأعمال التشغيلية، بينما الخادم الخاص يعطي تحكماً أكبر لكنه يحتاج تحديثات ومراقبة وحماية مستمرة. إذا كان المطلوب Docker أو تحكم نظامي كامل أو عنوان IP ثابت، فقد يلزم خادم سحابي مستقل؛ أما التطبيق الحالي فيمكن تشغيله عادةً على خدمة Node.js مُدارة.

### المرحلة الثانية: تجهيز البنية

جهّز المكونات التالية منفصلة عن بيئة Manus الحالية:

| المكوّن | المتطلب |
|---|---|
| التطبيق | Node.js بالنسخة المعتمدة في المشروع، وبناء `pnpm build` |
| قاعدة البيانات | MySQL/TiDB مُدارة، تشفير أثناء النقل، مستخدم بأقل صلاحيات |
| التخزين | Bucket خاص للملفات، روابط موقعة، منع الوصول العام |
| المصادقة | OAuth جديد أو إعداد منفصل، مع Redirect URI خاص بالبيئة الجديدة |
| الأسرار | Secret Manager، لا تُحفظ داخل المستودع |
| الشبكة | TLS، جدار ناري، تقييد منافذ قاعدة البيانات، وWAF أو rate limiting |
| المراقبة | سجلات، تنبيهات، فحص صحة، ومراقبة أخطاء دون تسجيل أسرار |
| النسخ | نسخ يومية مشفرة، احتفاظ متعدد المدد، واختبار استعادة |

### المرحلة الثالثة: إنشاء staging مستقلة

أنشئ مشروعاً باسم قريب من `tourism-statistics-staging` بقاعدة بيانات وOAuth وتخزين وأسرار منفصلة تماماً. انسخ نسخة بيانات منقحة أو نسخة اختبار، لا تنسخ البيانات الرسمية إلى مكان غير معتمد دون قرار مؤسسي. اجعل بيئة staging محمية بتسجيل دخول وقائمة مستخدمين مسموحة، ولا تربطها بالدومين العام.

استخدم GitHub Actions للبناء والاختبار، ويمكن تعريف بيئتين باسم `staging` و`production`. GitHub يدعم حماية البيئات، والمراجعين الإلزاميين، وتقييد الفروع، والأسرار الخاصة بكل بيئة [5]. لا تسمح بالدفع إلى `production` إلا بعد موافقة يدوية.

### المرحلة الرابعة: الترحيل التجريبي

1. خذ نسخة قاعدة حديثة.
2. استعدها في قاعدة staging.
3. شغّل migrations المتوافقة.
4. ارفع الملفات إلى التخزين الجديد مع الحفاظ على الصلاحيات.
5. اضبط متغيرات البيئة الجديدة.
6. اختبر تسجيل الدخول، الصلاحيات، الإدخال، المراجعة، الاعتماد، الخرائط، التنبؤ، التقارير، PDF، Excel، الدعم، والإشعارات.
7. نفّذ اختبار تحميل وأمن واستعادة.
8. سجّل الفروقات مع النسخة الحالية.

### المرحلة الخامسة: النقل إلى الإنتاج

بعد نجاح staging، خذ نسخة نهائية، أوقف الكتابة مؤقتاً إن لزم، نفّذ آخر مزامنة للقاعدة والملفات، ثم غيّر DNS إلى الخدمة الجديدة بعد تجهيز TLS. أبقِ الرابط القديم متاحاً داخلياً خلال فترة تحقق قصيرة، ولا تحذف البيئة القديمة حتى تتأكد من صحة البيانات وسلامة التقارير والاستعادة.

استخدم سياسة رجوع واضحة: إذا فشل تسجيل الدخول أو ظهرت فروقات في القياسات أو تعطلت التصديرات، أعد DNS إلى البيئة السابقة وأوقف التغييرات، ثم عالج السبب في staging.

## 9. قائمة قبول قبل النشر العام

لا تعتبر المنصة جاهزة للنشر العام إلا بعد تحقق الإدارة من الآتي:

- فصل كامل بين التطوير وstaging والإنتاج.
- قاعدة بيانات منفصلة لكل بيئة.
- OAuth وأسرار منفصلة لكل بيئة.
- نسخ احتياطية مشفرة واختبار استعادة ناجح.
- مراجعة صلاحيات admin وanalyst وviewer.
- عدم ظهور المسودات أو القياسات المرفوضة في التقارير الرسمية.
- مراجعة سجل التدقيق وعدم إمكانية الاعتماد الذاتي.
- فحص التبعيات والكود، ومعالجة الثغرات عالية الخطورة.
- حماية الخادم وقاعدة البيانات والتخزين والواجهات.
- اختبار جميع الأقسام والتصديرات والهواتف والمتصفحات.
- اعتماد قرار مكتوب من المدير العام قبل فتح الوصول الخارجي.

## 10. الخلاصة العملية لك الآن

في المرحلة الحالية، احتفظ بالرابط الداخلي كما هو، واحفظ الكود في المستودع الخاص، ونفّذ نسخة Task Data Backup من Manus بالإضافة إلى نسخة كود ونسخة قاعدة بيانات إن كان الاتصال متاحاً. عند تطوير ميزة، استخدم فرعاً جديداً وقاعدة اختبار واختبارات Vitest ومراجعة مستقلة. وعندما تقرر الإدارة الانتقال، أنشئ staging منفصلة أولاً، ثم انقل إلى الإنتاج بعد اختبار الاستعادة والحماية والموافقة الرسمية.

## المراجع

[1]: https://help.manus.im/en/articles/16147892-service-change-overview-how-to-back-up-your-data "Manus: How to Back Up Your Data"

[2]: https://help.manus.im/en/articles/16147831-service-change-overview-what-s-happening-and-am-i-affected "Manus: What’s Happening and Am I Affected?"

[3]: https://docs.github.com/articles/cloning-a-repository "GitHub Docs: Cloning a Repository"

[4]: https://dev.mysql.com/doc/refman/8.4/en/using-mysqldump.html "MySQL 8.4 Reference Manual: Using mysqldump for Backups"

[5]: https://docs.github.com/actions/deployment/about-deployments/deploying-with-github-actions "GitHub Docs: Deploying with GitHub Actions"
