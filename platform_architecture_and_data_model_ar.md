# وثيقة المعمارية ونموذج البيانات

## 1. الغرض ونطاق الوثيقة

تصف هذه الوثيقة البنية الفعلية لمنصة الإحصائيات والمؤشرات السياحية الوطنية، كما هي منفذة في المشروع. وهي مرجع فني لمركز المعلومات والتوثيق السياحي يوضح طبقات النظام، مسار البيانات، نموذج قاعدة البيانات، الصلاحيات، حدود النشر، ونقاط التشغيل التي يجب مراجعتها قبل أي انتقال إلى استضافة خارجية.

> **مبدأ المصدر الواحد:** لا تُعدّ أي قيمة رقماً رسمياً منشوراً إلا إذا كانت مرتبطة بمصدر موثق وحالة اعتماد `approved`. أما المسودة والمراجعة والتنبؤات فهي مراحل تشغيلية منفصلة ولا تُعرض كحقائق منشورة.

## 2. الصورة المعمارية

المنصة تطبيق ويب كامل مكوّن من واجهة React وخادم Express وإجراءات tRPC وقاعدة MySQL/TiDB يديرها Drizzle ORM. تتدفق العملية من المستخدم المصادق عليه عبر Manus OAuth إلى إجراء tRPC محمي، ثم إلى دوال طبقة البيانات، ثم تعود النتيجة typed إلى الواجهة. لا تستخدم الواجهة مسارات REST موازية؛ عقد tRPC هو العقد المشترك بين الطرفين.

| الطبقة | المكونات الفعلية | المسؤولية |
|---|---|---|
| العرض | `client/src/pages` و`client/src/components` | اللوحات، الإدخال، الخرائط، التقارير، المساعدة، الإعدادات والتصدير. |
| الحالة والتعاقد | `client/src/lib/trpc.ts` وReact hooks | استدعاء الإجراءات، حالات التحميل، الأخطاء، والتحديث التفاعلي. |
| الخادم | `server/_core` و`server/routers.ts` | Express، OAuth، سياق المستخدم، tRPC، التحقق، الصلاحيات، واستدعاء الخدمات. |
| منطق الأعمال | `server/db.ts` وملفات `server/*.ts` | الاستعلامات، الاعتماد، التنبؤ، السرد التحليلي، المساعد، الاستيراد، والتصدير المساعد للواجهة. |
| البيانات | `drizzle/schema.ts` و`drizzle/*.sql` | الجداول، العلاقات، الفهارس، وحركة الترحيلات. |
| التخزين | S3 عبر `storage.ts` وطبقة التخزين المدمجة | حفظ المرفقات والملفات الكبيرة مع تخزين البيانات الوصفية في قاعدة البيانات. |
| التكاملات | Manus OAuth وLLM وMaps والإشعارات | المصادقة، التحليل النصي المقيّد، الخرائط، والتنبيهات. |

## 3. الباك إند ومسار الطلب

تبدأ المصادقة من Manus OAuth، ويُنشئ سياق الطلب المستخدم الحالي. يمر طلب القراءة أو التعديل بإجراء tRPC مناسب. تستخدم الإجراءات العامة للعرض المكاني المنشور فقط، بينما تستخدم الإجراءات المحمية للوظائف الداخلية، ويُشترط دور `analyst` أو `admin` للإدخال والمراجعة. العمليات الإدارية الحساسة تستخدم قدرات منفصلة مثل `canManageRoles` و`canApproveReleases` و`canReviewSecurity`، ولا يكفي وجود دور `admin` وحده.

تستخدم مدخلات الخادم مخططات Zod للتحقق من النوع والنطاق والوحدة والفترة. تشمل الحماية التحقق من توافق `annual` مع `quarter = annual`، ومنع قيم خارج حدود المؤشر، ومنع الاستبدال غير المصرح به، ومنع المراجعة الذاتية، واشتراط انتقال القياس من `reviewed` إلى `approved` قبل النشر.

## 4. الفرونت إند ومسارات الاستخدام

تعمل الواجهة ضمن `DashboardLayout` للوظائف الداخلية، مع مسارات منفصلة للإدخال والمؤشرات والتنبؤ والأرشيف والاستيراد والتقارير والنشر والخرائط وإدارة المستخدمين ومراجعة الأمان والمساعدة. تعرض واجهات الخرائط والنشر العام ما تسمح به إجراءات القراءة العامة، فيما تبقى عمليات الإدخال والاعتماد والإدارة خلف المصادقة.

تستخدم الواجهة مكونات جاهزة للخرائط والدردشة والحوار والتقدم، وتدعم RTL والوضع الليلي وإمكانية الوصول وتقليل الحركة. التصدير إلى PDF وExcel يتم من المتصفح مع إضافة سياق المصدر وحالة الاعتماد عند توفره. لا تحفظ المنصة كلمات المرور محلياً؛ إدارة كلمة المرور والاسترداد تظل لدى Manus OAuth.

## 5. قاموس قاعدة البيانات

| المجموعة | الجداول | الغرض والضوابط |
|---|---|---|
| الهوية والصلاحيات | `users`, `userPreferences`, `administrativeMembers`, `administrativeAccessEvents` | المستخدمون، التفضيلات، القائمة الإدارية المصرح لها، وسجل منح وتعليق الصلاحيات. |
| أمن التبعيات | `dependencyReviewRuns`, `dependencyReviewSchedules` | نتائج مراجعة التبعيات وجدولة المراجعة؛ التفعيل الإنتاجي لا يتم تلقائياً. |
| الدعم | `supportRequests`, `supportRequestReplies`, `supportRequestAttachments`, `supportNotifications`, `helpContentRatings` | التذاكر والردود والمرفقات والإشعارات وتقييم محتوى المساعدة. تحفظ المرفقات في S3. |
| المؤشرات | `indicators` | كود المؤشر واسمه ووصفه ومحوره وإطاره ووحدته وطريقة حسابه ومصدره وحالة نشره. |
| القياسات الوطنية | `indicatorObservations` | القيمة والقيمة المستهدفة والسنة والفترة والربع والمصدر والملاحظات وحالة التحقق والمدخل والمراجع. |
| القياسات المكانية | `spatialAreas`, `spatialObservations`, `spatialObservationReviewEvents` | المدن والمناطق والقياسات المكانية وسجل انتقال حالتها. ترتبط القياسات بالمؤشر والمكان. |
| النشر | `publicationDestinations` | وجهات النشر وحالتها وتحديثها؛ لا تُفعّل وجهة خارجية قبل تهيئة عقد ومصادقة وموافقة. |
| الاستيراد | `importJobs`, `importIssues` | دفعات الاستيراد ومشكلات التحقق الناتجة عنها، مع بقاء البيانات غير المعتمدة خارج النشر. |

## 6. العلاقات وقواعد الحذف

ترتبط القياسات الوطنية بالمؤشرات، والقياسات المكانية بالمؤشرات والمناطق، وترتبط التذاكر بالردود والمرفقات والإشعارات. تستخدم بعض العلاقات `cascade` للكيانات التابعة مثل تفضيلات المستخدم ومرفقات التذكرة، بينما تستخدم `restrict` لحماية السجل المرجعي مثل المؤشرات والمناطق عند وجود قياسات، وتستخدم `set null` للحفاظ على السجل عند حذف مستخدم لم يعد متاحاً.

## 7. دورة حياة البيانات

يمر القياس الوطني أو المكاني بالمراحل التالية: إدخال أو استيراد كمسودة، تحقق آلي من الحقول والوحدة والفترة، مراجعة من محلل مستقل، اعتماد إداري، ثم دخوله في اللوحات والتقارير والنشر. القياس المرفوض لا يعود إلى مسودة إلا بواسطة مسؤول وبشروط مقيدة. القياسات المعتمدة لا تُعدّل باستبدال صامت؛ أي تصحيح يجب أن يكون دفعة جديدة أو مسار تعديل موثق وفق سياسة المركز.

التنبؤات تُحسب من القياسات السنوية المعتمدة فقط، وتُعرض بوسم واضح بأنها تقديرات. لا يجوز نسخ التنبؤ إلى جدول القياسات الرسمية دون مصدر ومراجعة واعتماد مستقلين.

## 8. الهيكل التنظيمي المقترح والمتوافق مع الصلاحيات

| الدور المؤسسي | الدور التقني | المسؤوليات |
|---|---|---|
| مسؤول الأمن | `admin` مع `canReviewSecurity` | مراجعة الصلاحيات، الأمن، التبعيات، السجلات، والجاهزية التشغيلية؛ لا يعتمد أرقاماً إلا إذا كان مخولاً بذلك منفصلاً. |
| رئيس الإحصاء | `admin` مع `canApproveReleases` | اعتماد القياسات المراجعة، اعتماد الإصدارات والوجهات وفق التفويض، وإصدار القرار الإحصائي. |
| محلل/مدخل إحصائي | `analyst` | إدخال واستيراد المسودات، معالجة أخطاء التحقق، والمراجعة المستقلة لقياسات الآخرين. |
| قارئ داخلي | `viewer` | القراءة والتحليل والتقارير والمساعدة ضمن نطاق الوصول. |
| مالك المنصة | Owner | التهيئة الأولية للقدرات الإدارية الحساسة وإدارة القرار الأعلى. |

يجب أن يكون فصل الواجبات عملياً: لا يدخل الشخص القياس ثم يراجعه بنفسه، ولا يعتمد القياس قبل وجود مراجعة مستقلة. يُفضل إصدار قائمة تفويض داخلية بأسماء الأعضاء وفترة التفويض ومراجعتها دورياً.

## 9. الأمن والتشغيل

تطبق المنصة رؤوس حماية تتضمن CSP أساسية، و`nosniff`، وسياسة مرجع، وPermissions Policy، إضافة إلى تحديد معدل الطلبات على مستوى عنوان العميل. يجب اعتبار هذه حماية تطبيقية أولية وليست بديلاً عن جدار حماية، VPN، نسخ احتياطي، مراقبة مركزية، وإدارة أسرار لدى شركة الاستضافة عند النشر الخارجي.

لا تُرفع أسرار OAuth أو قاعدة البيانات إلى GitHub. لا تُنفذ تحديثات تبعيات تلقائية على البيئة الحية. يجب إنشاء نسخة اختبار مستقلة عند الانتقال إلى دورة نشر احترافية، ثم إجراء الفحوصات والقبول قبل اعتماد الإصدار.

## 10. حدود الجاهزية المعروفة

المنصة جاهزة للتشغيل الداخلي الخاص ضمن الأسرار والبنية الحالية، لكنها ليست إعلاناً بأن جميع متطلبات الاستضافة الخارجية قد اكتملت. يلزم قبل النشر العام تأكيد اسم النطاق، TLS، VPN أو قائمة وصول، نسخ احتياطي قابل للاستعادة، مراقبة، سياسة احتفاظ، اختبار اختراق، فصل بيئة الاختبار، واتفاقية مسؤولية مع مزود الاستضافة. كما يلزم تثبيت عقد نشر رسمي لـ Visit Libya وأطلس ليبيا قبل فتح أي endpoint خارجي.

## 11. مراجع تنفيذية داخل المشروع

- مخطط البيانات: `drizzle/schema.ts`.
- عقود الخادم والصلاحيات: `server/routers.ts` و`server/_core/trpc.ts`.
- طبقة البيانات: `server/db.ts`.
- حماية HTTP وتحديد المعدل: `server/_core/security.ts`.
- المسارات: `client/src/App.tsx`.
- دليل التشغيل والنسخ الاحتياطي: `deployment-backup-development-guide-ar.md`.
- سياسة الاستخدام الداخلي: `internal_use_and_publication_policy.md`.
- تقرير الأمان: `security_audit_report_2026-08-26.md`.
- سجل المصدر الرسمي للحدود: `official-boundary-source-review.md`.

> هذه الوثيقة تصف الكود الموجود ولا تمنح وحدها تفويضاً تنظيمياً أو اعتماداً إحصائياً. أي قرار نشر أو اعتماد بيانات يجب أن يصدر من الجهة المخولة في المركز.
